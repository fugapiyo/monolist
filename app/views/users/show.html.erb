<% content_for :item_view_show_title do %>
  <%= @user.name %>のページ - Monolist
<% end %>
<% content_for :item_view_show_meta do %>
  <meta name="description" content="<%= @user.name %>が所持している、もしくは欲しいと思っている商品を表示しています。" />
<% end %>

<%= render 'profile' , user: @user %>


<%= render 'items/list' , items: @items %>